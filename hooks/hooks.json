{
  "description": "cc-hero: /hero opens a pane that scrolls a song as tab and staff notation and scores what the microphone hears. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": ["./register.tsx"]
}
